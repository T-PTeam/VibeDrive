import { Audio, InterruptionModeIOS, InterruptionModeAndroid } from 'expo-av';
import { logger } from './LoggerService';

export async function configureAudioSessionForHandsFree(): Promise<void> {
  logger.info('AudioSession', 'configureAudioSessionForHandsFree');
  await Audio.setAudioModeAsync({
    allowsRecordingIOS: true,
    playsInSilentModeIOS: true,
    staysActiveInBackground: true,
    interruptionModeIOS: InterruptionModeIOS.DoNotMix,
    interruptionModeAndroid: InterruptionModeAndroid.DoNotMix,
    shouldDuckAndroid: true,
    playThroughEarpieceAndroid: false,
  });
}

export async function configureAudioSessionForHandsFreeCapture(): Promise<void> {
  logger.info('AudioSession', 'configureAudioSessionForHandsFreeCapture');
  await Audio.setAudioModeAsync({
    allowsRecordingIOS: true,
    playsInSilentModeIOS: true,
    staysActiveInBackground: true,
    interruptionModeIOS: InterruptionModeIOS.DuckOthers,
    interruptionModeAndroid: InterruptionModeAndroid.DuckOthers,
    shouldDuckAndroid: true,
    playThroughEarpieceAndroid: false,
  });
}

export async function configureAudioSessionForPlayback(): Promise<void> {
  logger.info('AudioSession', 'configureAudioSessionForPlayback');
  await Audio.setAudioModeAsync({
    allowsRecordingIOS: false,
    playsInSilentModeIOS: true,
    staysActiveInBackground: false,
    interruptionModeIOS: InterruptionModeIOS.DoNotMix,
    interruptionModeAndroid: InterruptionModeAndroid.DoNotMix,
    shouldDuckAndroid: false,
    playThroughEarpieceAndroid: false,
  });
}
